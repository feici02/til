Examples

```
$ git clone https://github.com/hollenberry/art-place.git
cd art-place

$ git branch
* master

$ git branch -a
* master
remotes/origin/HEAD -> origin/master
remotes/origin/atomic-commits
remotes/origin/examples
remotes/origin/gallery
remotes/origin/master
remotes/origin/merge-conflict
remotes/origin/simple-color

$ git checkout simple-color
Branch simple-color set up to track remote branch simple-color from origin.
Switched to a new branch 'simple-color'
```
